import { Component } from '@angular/core';

import { Platform } from '@ionic/angular';
import { SplashScreen } from '@ionic-native/splash-screen/ngx';
import { StatusBar } from '@ionic-native/status-bar/ngx';
import { Crashlytics } from '@ionic-native/fabric/ngx';

@Component({
  selector: 'app-root',
  templateUrl: 'app.component.html'
})
export class AppComponent {
  constructor(
    private platform: Platform,
    private splashScreen: SplashScreen,
    private statusBar: StatusBar,
    private crashlytics: Crashlytics
  ) {
    this.initializeApp();
  }

  initializeApp() {
    this.platform.ready().then(() => {
      this.statusBar.styleDefault();
      this.splashScreen.hide();
      this.crashlytics.setUserName('DEVELOPER_NAME');
      this.crashlytics.setUserIdentifier('Dev'); // You can set the identifier as per your choice
      this.crashlytics.setUserEmail('DEVELOPER_EMAIL');
    });
  }
}
